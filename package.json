{
  "name": "novel-translator",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "./start.bat",
    "sanitize": "bun tools/sanitize_all.ts",
    "tools": "bun tools/tools.ts",
    "typecheck": "bun run tsc --noEmit",
    "format": "bun run prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/jsdom": "^30.0.0"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  }
}
